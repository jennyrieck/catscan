# catscan 0.2.0

## New Features

* `gc_fiscal_year` and `fy` functions added
* testing with `testthat`
* added vignette `cat-search-template`

# catscan 0.1.1

## Bug Fixes

* input validation for `wb_` helpers

# catscan 0.1.0

* Initial stable release
